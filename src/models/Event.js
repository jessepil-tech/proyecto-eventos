export default class Event {
  constructor({ title, description, date, capacity }) {
    this.title = title;
    this.description = description;
    this.date = date;
    this.capacity = capacity;
  }
}
